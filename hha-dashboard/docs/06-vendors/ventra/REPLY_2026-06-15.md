# Reply to Ventra — "Follow Up on Review of BI & Data"

Draft for Akhil to send (reply-all to Gilda + Dinesh + the existing thread).
Answers both red-ink ASKs, acknowledges the keys, and lists the
clarifications HHA needs before the first test drop.

---

**Subject:** RE: Follow Up on Review of BI & Data

Hi Gilda, Dinesh,

Thanks — we have the two public key files (`ventra-stdspec.pub` and
`ventra-preagg.pub`) and they're installed on our end. Answers to the two
items below, plus a few clarifications so the first drop validates cleanly.

**(a) FL facilities — confirmed: Yes.** All seven are correct and map to
our Florida sites:

| FacilityID | Facility | Our site |
|---|---|---|
| 2284 | Jackson Memorial Hospital | Jackson Memorial |
| 2285 | HCA Florida JFK Main Hospital | JFK Main |
| 2286 | HCA Florida JFK North Hospital | JFK North |
| 2287 | HCA Florida Palms West Hospital | Palms West |
| 2288 | HCA Florida University Hospital | University |
| 2289 | HCA Florida Westside Hospital | Westside |
| 2290 | HCA Florida Woodmont Hospital | Woodmont |

**(b) Files requested — 1-4 plus 5 (TransactionsAlt):**

1. Invoice
2. ChargeLines
3. Physician
4. Facility
5. **TransactionsAlt**

We need TransactionsAlt in addition to 1-4 because that's the file that
carries payments, adjustments, and refunds — without it we can see charges
and RVUs but not collections, which is the core of what we're reporting on.
Please **exclude** the rest (TransactionsAlt aside, we do not need
DownCodes, HeldCharts, ChartEntry, Guarantor, or Carrier).

On the patient-level detail in Invoice/ChargeLines: our platform
aggregates these at the edge and never stores patient identifiers, so
you're welcome to send the standard file as-is — we drop the PHI columns
on ingest and persist only the rolled-up financials per facility, payer
class, and physician.

**SFTP connection details** (dev endpoint — live now):

- Host: `sthhavendordev5801224b.blob.core.windows.net` (port 22)
- Standard Data Extract user: `sthhavendordev5801224b.ventrastdspec` → path
  `/vendor-inbound/ventra/stdspec/YYYY-MM-DD/`
- Pre-aggregated user: `sthhavendordev5801224b.ventrapreagg` → path
  `/vendor-inbound/ventra/preagg/YYYY-MM-DD/`
- Auth: SSH key only (your `ventra-stdspec` / `ventra-preagg` public keys
  are installed).

One firewall item: the endpoint is locked to an IP allowlist (HIPAA
posture — it's not open to the internet). **Please send us the egress IP
address(es) your SFTP client will connect from** and we'll allowlist them
so your test drop can land. (We've allowlisted our own IP for an initial
connectivity check.)

Please write `_MANIFEST.csv` **last** in each daily folder (it's our trigger
file) with the columns `file_name, sha256, row_count`.

**A few clarifications so the first drop validates cleanly:**

1. **File names + format** — exact delivered filenames + extension (e.g.
   `Invoice.csv`?) and the delimiter (comma vs pipe)? We'll match whatever
   you send; just want the manifest `file_name` to line up.
2. **Insurance class values** — what's the value set for `PrimaryInsClass` /
   `InsuranceClass`? We map them to commercial / Medicare / Medicaid /
   self-pay / other and want the mapping right from day one.
3. **Transaction amount sign** — are payments positive or negative in
   `TranAmt` (and adjustments/refunds)? Just confirming the convention.
4. **Pre-aggregated feed** — will it also be keyed by FacilityID 2284-2290
   (same as the standard extract)? Assuming yes.

Whenever you're ready, send a small test drop to the stdspec path and we'll
confirm it lands and validates end-to-end.

Thanks,
Akhil

---

## Operator checklist (not part of the email)

- [ ] `az login --tenant 76596b76-3c41-40ee-a8a3-bf6930301838`
- [ ] `.\scripts\deploy-ventra-sftp.ps1 -ImportKeys docs\06-vendors\ventra\sftp-keys -WhatIfOnly` (preview)
- [ ] `.\scripts\deploy-ventra-sftp.ps1 -ImportKeys docs\06-vendors\ventra\sftp-keys` (apply)
- [ ] Paste the printed SFTP host + usernames into the email's connection block
- [ ] Send the reply
