# Reply to Ventra — "Follow Up on Review of BI & Data" (round 2)

Draft for Akhil to send (reply-all to Gilda + Dinesh + the existing
thread). Answers Ventra's five red-ink items a–e from their reply (their
office was closed Fri 6/19; reply landed ~6/22), confirms the zip +
filename + signed-amount contract, and lists the one clarification still
open before the first test drop.

> Note: filename keeps the `REPLY_2026-06-15.md` thread anchor; the content
> answers the latest follow-up. Rename to `REPLY_2026-06-22.md` if you
> prefer a per-round file.

---

**Subject:** RE: Follow Up on Review of BI & Data

Hi Gilda, Dinesh,

Thanks for the detailed follow-up — answers to a–e below. Net: we're good
to go on all five, with one small clarification on the amount signs and one
on the insurance-class values so the first drop reconciles cleanly.

**(a) PHI / aggregation — confirmed: Yes.** That quote is exactly right.
Our platform aggregates Invoice, ChargeLines, and TransactionsAlt **at the
ingest edge** and never stores patient identifiers — we drop every PHI
column on the way in and persist only the rolled-up financials per
facility, payer class, and physician. Nothing patient-level ever lands in
our database, logs, or reporting. So please **send the standard extract
as-is, unmasked** — no PHI masking needed on your side.

**(b) SFTP — thank you.** When your IT team is ready, here is the dev
endpoint to point them at:

- Host: `sthhavendordev5801224b.blob.core.windows.net` (port 22)
- Standard Data Extract user: `sthhavendordev5801224b.ventrastdspec`
- Auth: SSH key only — your `ventra-stdspec.pub` key is already installed.

One firewall item: the endpoint is locked to an IP allowlist (HIPAA
posture — deny by default). **Please send us the public egress IP(s) your
SFTP client will connect from** and we'll allowlist them before your first
push.

**(c) Zip instead of a manifest — OK, that works for us.** A single zip is
actually cleaner on our side: we trigger ingestion when the zip lands and
use the zip's own integrity check, so we don't need `_MANIFEST.csv`. Two
quick confirmations so we wire the trigger correctly:

1. The zip filename pattern is stable and the date in the name is the
   extract/business date — we saw `HHA_Extact_20260610.zip`; we'll read
   that `YYYYMMDD` as the drop date. (No worries on the spelling, we'll
   match it either way.)
2. The zip appears on the SFTP folder **only once it's fully written** (so
   we never pick up a partial file mid-upload). Your note — "HHA can pull
   the file once it's fully imported" — suggests this is already the case;
   just confirm the mechanism (e.g. upload-then-rename, or your client only
   exposes the file on completion).

**(d) File names + format — OK, confirmed.** Five comma-separated CSVs:
`Invoice.csv`, `ChargeLines.csv`, `Physician.csv`, `Facility.csv`,
`TransactionsAlt.csv`. One ask: please confirm each file has a **header
row** with the column names from the spec (we match column names
case-insensitively, so casing differences are fine).

**(e) TranAmt sign — OK, signed amounts are exactly what we want.** We sum
the signed `TranAmt` natively, so positive/negative is perfect. To make
sure our net-collections math lines up with your books, please confirm the
sign convention by transaction type:

- Payment → positive?
- Refund → negative?
- Adjustment (Contractual / Bad Debt) → sign?
- Transfer → how is it signed, and should it net to zero across invoices?

**One more clarification (carryover): insurance-class values.** Please send
the list of distinct values you emit for `InsuranceClass` /
`PrimaryInsClass` (e.g. the exact strings for commercial, Medicare,
Medicaid, self-pay, etc.). We map those to our payer classes; anything we
don't recognize we'll bucket as "other," so the value list lets us map them
all correctly up front.

That's everything on our end — we're ready to receive your test zip as soon
as the SFTP IP allowlist and the items above are squared away. Looking
forward to the first drop.

Thanks,
Akhil

---

## Internal notes (do not send)

- **a** answers the green-highlighted PHI quote — confirms HHA's edge-aggregation / no-PHI posture so Ventra ships unmasked.
- **c** is the big one: Ventra is dropping `_MANIFEST.csv` for a single zip. Pipeline change tracked in plan Phase 4Z (C1 Event Grid `.zip` trigger, C2 zip loader replaces manifest V1–V4, dedup on zip sha256).
- **e**: aggregator switches from `abs()` to signed-sum (plan C4). Need Ventra's per-`TranType` sign convention to finalize the payments/refunds/adjustments mapping; default assumption: Payment +, Refund −, reversals negative.
- Open item still outstanding after this reply: `InsuranceClass` value vocabulary (payer-class mapping). Everything else (filenames, delimiter, TranAmt signed) is now CONFIRMED.
- Dev endpoint firewall is deny-by-default — blocked on Ventra's egress IP(s).
